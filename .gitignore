# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# Custom application binaries (based on your 'bin' folder)
bin/

# Environment Variables
.env
.env.local

# OS Specific
.DS_Store

# IDEs
.idea/
.vscode/
*.swp
*.swo

# Docker / Postgres data (if mounted locally in the future)
db-data/
pgdata/
